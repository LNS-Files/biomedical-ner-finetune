# Core training stack
torch>=2.2                     # Kaggle ships PyTorch; local installs should use a Python-compatible build
transformers==4.40.2           # 4.40+ required for PEFT integration fixes in token classifiers
datasets==2.18.0               # BC5CDR bigbio config is stable from this version
bioc==2.1                      # Required by the BigBio BC5CDR dataset loader
peft==0.10.0                   # LoRA for encoder models landed cleanly in 0.10
accelerate==0.29.3             # Required by PEFT trainer; must be compatible with transformers
bitsandbytes==0.44.0           # 0.44+ removes the triton.ops dependency that breaks on Kaggle's Python 3.12

# Evaluation
seqeval==1.2.2                 # Standard NER metric library (span-level F1)

# Experiment tracking
wandb==0.16.6                  # Free tier works on Kaggle; set WANDB_MODE=offline if needed

# Utilities
numpy==1.26.4                  # Pin to <2.0 — several deps still have numpy 2.x issues
scikit-learn==1.4.2            # seqeval dependency
tqdm==4.66.2
huggingface-hub==0.22.2        # For saving/loading adapters to HF Hub

# Notebook (Kaggle environment)
ipykernel==6.29.4
ipywidgets==8.1.2
